# Copia de referencia de `.env.local` (el archivo real NO se sube a GitHub).
# Si trabajas desde otra computadora: copia este archivo como `.env.local` y
# llena los dos valores desde supabase.com → Project Settings → API.
# Instrucciones completas en FASE2.md.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
