/node_modules
/.next
/out
/build
.env*.local
.env
*.log
.DS_Store
next-env.d.ts
.vercel
*.tsbuildinfo

# Archivos fuente de 3D pesados (no usados en la web; se editan en 3ds Max)
*.max
*.rar
.dev-server.log

# Documentos con datos de clientes reales (el repo es público: NUNCA subirlos)
*.pdf
